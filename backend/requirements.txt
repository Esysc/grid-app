# Core dependencies
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0
sqlalchemy==2.0.45
asyncpg==0.31.0
python-dotenv==1.2.1
python-multipart==0.0.21  # Required for form parsing in Starlette

# Authentication
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt<5.0.1

# GraphQL
strawberry-graphql[asgi]==0.289.0

# AWS/S3
boto3==1.42.28
botocore==1.42.28
boto3-stubs[s3]==1.42.28  # Type stubs for boto3

# MQTT for sensor simulation
aiomqtt==2.5.0

# Development dependencies (optional)
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Or: pip install ".[dev]"
